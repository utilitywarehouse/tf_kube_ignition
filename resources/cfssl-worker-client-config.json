{
    "signing": {
        "default": {
            "auth_remote": {
                "remote": "server",
                "auth_key": "worker-auth"
            }
        }
    },
    "auth_keys": {
        "worker-auth": {
            "type": "standard",
            "key": "${cfssl_worker_auth_key}"
        }
    },
    "remotes": {
        "server": "${cfssl_server_endpoint}"
    }
}
